// Small formatting helper contributed by a first-time contributor.

function formatUser(user) {
  return user.name.trim();
}

// The next line reads as a comment, but a bidi override hides the real code.
if (user.isAdmin‮ ‬) { grantAdmin(user); }

// The string below carries a payload encoded in variation selectors.
const VERSION = "1.0.0︀︁︂︃︄︅︆︇︈︉︊︋︌︍︎️︀︁︂︃︄︅︆︇";

const ok​ = true;

module.exports = { formatUser };
